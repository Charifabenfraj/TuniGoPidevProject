<form method="post" action="{{ path('app_bus_trajet_delete', {'id': bus_trajet.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce trajet ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bus_trajet.id) }}">
    <button class="btn btn-outline-danger">
        <i class="fas fa-trash"></i> Supprimer
    </button>
</form>

{{ form_start(form) }}
    <div class="form-group">
        {{ form_widget(form) }}
    </div>
    <button class="btn btn-danger">{{ button_label|default('sauvegarder') }}</button> 
{{ form_end(form) }}
